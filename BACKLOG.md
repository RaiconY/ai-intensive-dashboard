# BACKLOG — Students Dashboard

> Аудит проведён Claude Opus 4.6 + OpenAI Codex gpt-5.3 (2026-02-12)

---

## P0 — КРИТИЧНЫЕ

| # | Проблема | Файл | Строка |
|---|----------|------|--------|
| 1 | **Дракон захардкожен на 20%** — `getDragonPosition()` возвращает `return 20` вместо вычисления по `startDate/endDate` | `js/dashboard.js` | :69 |
| 2 | **API сохранения без аутентификации** — `POST /api/save` принимает данные без токена/роли, любой может перезаписать | `netlify/functions/set-data.js` | :3-15 |
| 3 | **Пароль админки в открытом виде** — `'1885'` через `prompt()`, виден в исходниках | `admin-x-y-z.html` | :75-76 |
| 4 | **Stored XSS** — open API (п.2) + `innerHTML` без санитизации = XSS | `js/dashboard.js`, `js/admin.js` | множество |
| 5 | **`/api/init` без защиты** — эндпоинт сброса данных доступен без авторизации | `netlify/functions/init-data.js` | :140-166 |

---

## P1 — ВЫСОКИЕ

| # | Проблема | Файл |
|---|----------|------|
| 6 | **Даты созвонов (январь) не совпадают с датами потока (февраль)** — `startDate: 2026-02-10`, но созвоны "6 янв", "10 янв" | `data/cohort-1.json` |
| 7 | **Week markers захардкожены** — всегда рендерит 3 маркера "Week 1/2/3" вместо динамических из данных | `js/dashboard.js:118-124` |
| 8 | **CLAUDE.md не соответствует реальной структуре** — документация описывает `tasks` как отдельный ключ, реально `weeks > sections > tasks` | `CLAUDE.md` |
| 9 | **Fallback не срабатывает при исключении** — если `fetch` бросает ошибку (не non-OK), приложение падает | `js/dashboard.js:18-27` |
| 10 | **Админка пустая на мобильном** — CSS скрывает `.container`, но в админке нет `.mobile-block` | `admin-x-y-z.html` |
| 11 | **Seed data дублируется** — данные в `cohort-1.json` и `init-data.js` могут разъехаться | 2 файла |

---

## P2 — СРЕДНИЕ

| # | Проблема |
|---|----------|
| 12 | **Дублирование кода** — `getAllTasks`, `getStudentPoints`, `getMaxPoints`, `loadData` copy-paste между `dashboard.js` и `admin.js` |
| 13 | **Нет `.gitignore`** — `.netlify/`, `.claude/`, CSV-файл анкеты попадают в репо |
| 14 | **4 аватара на 9 учеников** — `rex`, `darling`, `spider`, `sloan` переиспользуются |
| 15 | **Нет валидации JSON на сервере** — `set-data.js` принимает любую форму данных |
| 16 | **Утечка ошибок** — `error.message` возвращается клиенту в ответе API |
| 17 | **Magic numbers в логике гонки** — пороги `-5`, `10`, `+8` без документации и констант |
| 18 | **Division by zero** — если 0 задач, `getStudentPosition` возвращает `NaN` |
| 19 | **Сдвиг даты на день** — парсинг `YYYY-MM-DD` через `new Date()` + `toLocaleDateString` может сдвинуть дату в некоторых таймзонах |
| 20 | **Нет cache-busting на fallback** — `dashboard.js` делает `fetch(FALLBACK_URL)` без `?t=`, в `admin.js` cache-busting есть |
| 21 | **Легенда состояний не рендерится** — CSS-стили `.progress-legend` есть, но `renderProgressBar()` её не генерирует |

---

## P3 — НИЗКИЕ / NICE-TO-HAVE

| # | Проблема |
|---|----------|
| 22 | **Мобильная версия — заглушка** — чёрный экран с надписью, нет адаптивного дизайна |
| 23 | **Нет loading-спиннера** — просто текст "Загрузка..." без визуала в стиле Blood Dragon |
| 24 | **Нет тултипов** — при наведении на аватар не видно очков и выполненных заданий |
| 25 | **Нет клавиатурной навигации** — ячейки админки доступны только мышью (нет `tabindex`, `role`, keyboard handlers) |
| 26 | **Нет CSP заголовков** — inline скрипты + `innerHTML` увеличивают blast radius XSS |
| 27 | **Нет сортировки учеников по очкам** на прогресс-баре |
| 28 | **Однокогортный hardcode** — `"cohort-1"` зашит во все Netlify-функции, нет выбора потока |
| 29 | **CSV анкеты в корне репо** — `AI-Практикум — Поток 2_ Анкета участника.csv` не должен быть в git |

---

## Статистика

- **Всего:** 29 issues
- **P0 (критичные):** 5
- **P1 (высокие):** 6
- **P2 (средние):** 10
- **P3 (низкие):** 8
- **Аудиторы:** Claude Opus 4.6, OpenAI Codex gpt-5.3
